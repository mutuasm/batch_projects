# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Frontend dependencies — never commit node_modules
node_modules/

# Frontend generated/cache — auto-generated, not needed in repo

# Frontend build output — KEEP TRACKED so client installs work without Node

# Misc
dist/
*.log
.env
.env.*

# Internal planning/audit/handoff material — never public. Not deleted from
# disk (may still be useful locally), just never tracked. See CONTRIBUTING.md
# for what does ship.
WORKPLAN*
docs/
currentStatus.txt
claude-chat.html
*_code.zip
files_tab_wip.md
AGENTS.md