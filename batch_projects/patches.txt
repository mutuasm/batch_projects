[pre_model_sync]
batch_projects.patches.migrate_to_workflow_states
batch_projects.patches.backfill_board_rank
batch_projects.patches.normalize_project_member_roles
batch_projects.patches.create_project_work_activity_type

[post_model_sync]
# migrate_custom_fields_to_library reads/writes the new BP Custom Field /
# BP Custom Field Project tables, so it must run AFTER doctype schema sync,
# not before. configparser (which parses this file) requires every entry to
# sit under an explicit section once any section header is used — mixing
# headerless lines with a later [section] throws MissingSectionHeaderError,
# which Frappe silently mis-recovers from by treating the WHOLE file as a
# flat list (the bracketed header line itself included as a bogus "patch"),
# hence the "App [post_model_sync] is not installed" crash. Every patch must
# have an explicit section — no headerless lines anywhere in this file.
batch_projects.patches.migrate_custom_fields_to_library
# Needs erpnext's Accounting Dimension machinery + synced BP Task schema.
batch_projects.patches.create_bp_task_accounting_dimension
# Phase 20: needs the new actions/scope JSON columns synced first.
batch_projects.patches.migrate_automation_actions
# Phase 22D: needs the new scope/visibility columns synced first.
batch_projects.patches.migrate_report_scope_visibility
# Phase 43: clear false-positive triage flags from old schema default.
batch_projects.patches.backfill_triage_flags
# BP-04 (2026-07-27): re-name existing BP Intake Forms off their guessable
# form_title-derived names onto random hash names — the name IS the public
# /intake/<name> URL. Must run after the doctype's autoname config is
# already synced to "hash" (post_model_sync).
batch_projects.patches.rename_intake_forms_to_random
# Dashboard grid col-num 12->48 (DashboardView.vue) — rescale existing
# layouts' x/w/minW by the same 4x so widgets keep their size/position.
batch_projects.patches.rescale_dashboard_layout_columns
# Audit fix (2026-08-09): "BP Member" role fixture existed but was never
# granted DocPerm nor assigned to members, so permissions.py's project-scoped
# access hooks never ran for anyone without an unrelated ERPNext role — see
# access.ensure_member_role() and this patch's own docstring.
batch_projects.patches.grant_bp_member_baseline_docperm
# Deleting a User leaves its BP Project Member rows behind, and Frappe
# link-validates the whole doc on save — so one orphan makes the entire
# BP Project unsavable (LinkValidationError), including edits that never
# touch the members table. See the patch docstring.
batch_projects.patches.clean_orphaned_project_members
# BP Task v2 fields (2026-08-14): sequence_no (global monotonic identity),
# planned_start/planned_end (scheduling plan vs due-date commitment),
# completed_by + blocked_reason/blocked_since/blocked_by. Backfills existing
# rows; must run after the new columns are synced.
batch_projects.patches.backfill_task_sequence_and_planned_dates
# BP Task Watcher watch_reason (2026-08-14): records why each watcher exists
# (manual/mentioned/assigned/commented/approval/automation). Existing rows
# get 'manual' — the original cause can't be reconstructed.
batch_projects.patches.backfill_watch_reason
# Durable graph execution identity.  Runs after DocType sync so existing
# workflows receive a stable revision/hash before Gateway can admit events.
batch_projects.patches.backfill_workflow_definition_identity
# Final v1.1 release fence for execution-source metadata. A pre-release site
# may already have logged the older backfill identity and then created more
# rows while write paths were still missing source; use a fresh patch name so
# every upgrading site gets one last idempotent normalization after the
# DocType-level write invariant is in place.
batch_projects.patches.backfill_execution_metadata_release

# P0 billing reservation (2026-08-18): Sales Invoice Timesheet.timesheet_detail
# is stock ERPNext Data without search_index. The reservation guard needs a
# narrow current/locking read on this field; persist search_index metadata and
# create the index after DocType schema synchronization.
batch_projects.patches.add_sales_invoice_timesheet_detail_index

# P0 milestone billing lifecycle (2026-08-19): old two-state model marked
# Draft Sales Invoices as "Invoiced". Reconcile existing milestones against
# authoritative ERPNext Sales Invoice docstatus after the DocType options sync.
batch_projects.patches.reconcile_milestone_invoice_lifecycle

# Runtime V2 compiles rules into durable executions too. Existing rule rows
# need the same immutable revision/hash binding workflows already carry.
batch_projects.patches.backfill_automation_rule_definition_identity

# telemetry_key's own unique index (BP Automation Run / BP Workflow Run) is
# safe to create as a brand-new all-NULL column — MySQL/MariaDB allow
# multiple NULLs in a unique index — but any row written before this patch
# needs a computed key, and two rows that would compute the SAME key are, by
# construction, duplicates from the exact get_value()-then-insert race this
# migration closes. Must run after DocType schema sync (the column has to
# exist first) and de-duplicate before ever writing a key twice.
batch_projects.patches.backfill_run_telemetry_keys

# Stage 1 of the native-doctype migration: BP field set added to ERPNext's own
# Project/Task as Custom Fields. Additive, idempotent, nothing reads them yet.
# post_model_sync: the native doctypes must be synced before fields attach.
batch_projects.patches.create_native_project_custom_fields


# Native-model activation. A no-op unless bp_use_native_doctypes is set in
# site_config; see the patch docstring and
# native_migration.dry_run_native_migration for a preview that writes nothing.
# Last on purpose: it depends on every doctype and custom field above it.
batch_projects.patches.activate_native_model
